using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Chiller : Enemy
{

    // Start is called before the first frame update
    void Start()
    {
        expAmt = 55;
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
